<UserControl x:Class="UniversityTransportSystem.Presentation.Views.Operations.SubscriptionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             RenderTransformOrigin="0.5,0.5">
    <UserControl.RenderTransform>
        <TranslateTransform X="0" Y="0"/>
    </UserControl.RenderTransform>
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Transport Subscription Management" Style="{StaticResource HeaderTitleStyle}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="btnAdd" Content="+ Add Subscription" Style="{StaticResource ActionButtonStyle}" Click="BtnAdd_Click" Height="40" Width="160"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardContentStyle}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="txtSearch" Style="{StaticResource SearchBoxStyle}" TextChanged="TxtSearch_TextChanged" Width="320" HorizontalAlignment="Left"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnEdit" Content="Edit" Style="{StaticResource SecondaryActionButtonStyle}" Margin="0,0,8,0" Width="90" Click="BtnEdit_Click"/>
                    <Button x:Name="btnDelete" Content="Delete" Style="{StaticResource DangerButtonStyle}" Margin="0,0,8,0" Width="90" Click="BtnDelete_Click"/>
                    <Button x:Name="btnRefresh" Content="Refresh" Style="{StaticResource GreenActionButtonStyle}" Width="100" Click="BtnRefresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource GlassCardStyle}" Padding="0">
            <DataGrid x:Name="dgItems"
                      Style="{StaticResource DataGridStyle}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      RowStyle="{StaticResource DataGridRowStyle}"
                      CellStyle="{StaticResource DataGridCellStyle}"
                      SelectionChanged="DgItems_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="TransportSubscriptionID" Binding="{Binding TransportSubscriptionID}" Width="Auto" MinWidth="60"/>
                    <DataGridTextColumn Header="StudentID" Binding="{Binding StudentID}" Width="Auto" MinWidth="80"/>
                    <DataGridTextColumn Header="TransportLineID" Binding="{Binding TransportLineID}" Width="Auto" MinWidth="80"/>
                    <DataGridTextColumn Header="StartDate" Binding="{Binding StartDate, StringFormat={}{0:yyyy-MM-dd}}" Width="Auto" MinWidth="100"/>
                    <DataGridTextColumn Header="EndDate" Binding="{Binding EndDate, StringFormat={}{0:yyyy-MM-dd}}" Width="Auto" MinWidth="100"/>
                    <DataGridTextColumn Header="SubscriptionStatus" Binding="{Binding SubscriptionStatus, Converter={StaticResource BoolToStatusConverter}}" Width="*" MinWidth="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
