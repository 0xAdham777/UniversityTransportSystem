<UserControl x:Class="UniversityTransportSystem.Presentation.Views.Operations.IncidentFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             RenderTransformOrigin="0.5,0.5">
    <UserControl.RenderTransform>
        <TranslateTransform X="0" Y="0"/>
    </UserControl.RenderTransform>
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="btnBack" Content="← Back" Style="{StaticResource SecondaryActionButtonStyle}" Height="36" Width="100" Click="BtnBack_Click" Margin="0,0,16,0"/>
            <TextBlock Grid.Column="1" Text="Incident Form" Style="{StaticResource HeaderTitleStyle}" VerticalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource GlassCardStyle}" Padding="28" MaxWidth="600" HorizontalAlignment="Left">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="TripID" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="1" x:Name="txtTripID" Style="{StaticResource TextBoxStyle}"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="ReportedByEmployeeID" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="1" x:Name="txtReportedByEmployeeID" Style="{StaticResource TextBoxStyle}"/>
                </Grid>

                <Grid Grid.Row="2" Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="IncidentTypeID" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="1" x:Name="txtIncidentTypeID" Style="{StaticResource TextBoxStyle}"/>
                </Grid>

                <Grid Grid.Row="3" Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="IncidentDescription" Style="{StaticResource LabelStyle}"/>
                    <TextBox Grid.Row="1" x:Name="txtIncidentDescription" Style="{StaticResource TextBoxStyle}" TextWrapping="Wrap" AcceptsReturn="True" Height="100"/>
                </Grid>

                <Grid Grid.Row="4" Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="IncidentDateTime" Style="{StaticResource LabelStyle}"/>
                    <DatePicker Grid.Row="1" x:Name="dpIncidentDateTime" Style="{StaticResource DatePickerStyle}"/>
                </Grid>

                <Grid Grid.Row="5" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" x:Name="btnSave" Content="Save" Style="{StaticResource ActionButtonStyle}" Click="BtnSave_Click" Width="110" Margin="0,0,8,0"/>
                    <Button Grid.Column="1" x:Name="btnUpdate" Content="Update" Style="{StaticResource ActionButtonStyle}" Click="BtnUpdate_Click" Width="110" Margin="0,0,8,0" Visibility="Collapsed"/>
                    <Button Grid.Column="2" x:Name="btnCancel" Content="Cancel" Style="{StaticResource SecondaryActionButtonStyle}" Click="BtnCancel_Click" Width="110"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
