<UserControl x:Class="UniversityTransportSystem.Presentation.Views.Geography.StationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.RenderTransform>
        <TranslateTransform/>
    </UserControl.RenderTransform>
    <Grid>
        <Border Style="{StaticResource GlassCardStyle}" Margin="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="24,20,24,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Stations" Style="{StaticResource HeaderTitleStyle}" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="txtSearch" Style="{StaticResource SearchBoxStyle}" TextChanged="TxtSearch_TextChanged" Margin="8,0"/>
                    <Button Grid.Column="2" x:Name="btnEdit" Content="Edit" Style="{StaticResource ActionButtonStyle}" Click="BtnEdit_Click" Width="70" Margin="4,0"/>
                    <Button Grid.Column="3" x:Name="btnDelete" Content="Delete" Style="{StaticResource DangerButtonStyle}" Click="BtnDelete_Click" Width="70" Margin="4,0"/>
                    <Button Grid.Column="4" x:Name="btnRefresh" Content="Refresh" Style="{StaticResource SecondaryActionButtonStyle}" Click="BtnRefresh_Click" Width="70" Margin="4,0"/>
                    <Button Grid.Column="5" x:Name="AddButton" Content="+ Add" Style="{StaticResource ActionButtonStyle}" Click="AddButton_Click" Width="80"/>
                </Grid>
                <DataGrid Grid.Row="1" x:Name="StationDataGrid" Style="{StaticResource DataGridStyle}" Margin="24,0,24,24">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding StationID}" Width="80"/>
                        <DataGridTextColumn Header="Station Name" Binding="{Binding StationName}" Width="150"/>
                        <DataGridTextColumn Header="Location Description" Binding="{Binding LocationDescription}" Width="*"/>
                        <DataGridTextColumn Header="Municipality ID" Binding="{Binding MunicipalityID}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding StationStatus}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
