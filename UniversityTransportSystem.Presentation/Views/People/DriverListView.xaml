<UserControl x:Class="UniversityTransportSystem.Presentation.Views.People.DriverListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             RenderTransformOrigin="0.5,0.5"
             Loaded="UserControl_Loaded">
    <UserControl.RenderTransform>
        <TranslateTransform X="0" Y="20"/>
    </UserControl.RenderTransform>
    <UserControl.Resources>
        <Storyboard x:Key="PageEnter">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>
    <Grid Margin="28,0,28,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,8,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Driver Management" Style="{StaticResource HeaderTitleStyle}" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" Text="Manage all driver records" Style="{StaticResource HeaderSubTitleStyle}" Margin="16,0,0,0" VerticalAlignment="Center"/>
            <Button Grid.Column="2" x:Name="btnAddDriver" Content="+ Add Driver" Style="{StaticResource ActionButtonStyle}" Height="40" Width="140" Click="BtnAddDriver_Click"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource GlassCardStyle}" Margin="0,0,8,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="OVERVIEW" Style="{StaticResource SectionTitleStyle}" Margin="0"/>
                    <TextBlock Grid.Row="1" x:Name="txtTotalDrivers" Text="0" FontSize="36" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,8,0,4"/>
                    <TextBlock Grid.Row="2" Text="Total Drivers" FontSize="13" Foreground="#9CB0D1"/>
                </Grid>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource GlassCardStyle}" Margin="8,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="STATUS" Style="{StaticResource SectionTitleStyle}" Margin="0"/>
                    <TextBlock Grid.Row="1" x:Name="txtActiveDrivers" Text="0" FontSize="36" FontWeight="Bold" Foreground="#1DE5A8" Margin="0,8,0,4"/>
                    <TextBlock Grid.Row="2" Text="Active Drivers" FontSize="13" Foreground="#9CB0D1"/>
                </Grid>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource GlassCardStyle}" Margin="8,0,0,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="ACTIONS" Style="{StaticResource SectionTitleStyle}" Margin="0"/>
                    <TextBlock Grid.Row="1" Text="Quick Actions" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,8,0,4"/>
                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                        <Button Content="Export" Style="{StaticResource SecondaryActionButtonStyle}" Height="28" FontSize="11" Padding="10,0" Margin="0,0,6,0"/>
                        <Button Content="Print" Style="{StaticResource SecondaryActionButtonStyle}" Height="28" FontSize="11" Padding="10,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardContentStyle}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="txtSearch" Style="{StaticResource SearchBoxStyle}" TextChanged="TxtSearch_TextChanged" Width="320" HorizontalAlignment="Left"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnEditDriver" Content="Edit" Style="{StaticResource SecondaryActionButtonStyle}" Margin="0,0,8,0" Width="90" Click="BtnEditDriver_Click"/>
                    <Button x:Name="btnDeleteDriver" Content="Delete" Style="{StaticResource DangerButtonStyle}" Margin="0,0,8,0" Width="90" Click="BtnDeleteDriver_Click"/>
                    <Button x:Name="btnRefresh" Content="Refresh" Style="{StaticResource GreenActionButtonStyle}" Width="100" Click="BtnRefresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="3" x:Name="dgDrivers"
                  Style="{StaticResource DataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  RowStyle="{StaticResource DataGridRowStyle}"
                  CellStyle="{StaticResource DataGridCellStyle}"
                  SelectionChanged="DgDrivers_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="DriverID" Binding="{Binding DriverID}" Width="100"/>
                <DataGridTextColumn Header="EmployeeID" Binding="{Binding EmployeeID}" Width="100"/>
                <DataGridTextColumn Header="LicenseNumber" Binding="{Binding LicenseNumber}" Width="150"/>
                <DataGridTextColumn Header="LicenseExpiryDate" Binding="{Binding LicenseExpiryDate, StringFormat={}{0:yyyy-MM-dd}}" Width="140"/>
                <DataGridTextColumn Header="DriverStatus" Binding="{Binding DriverStatus, Converter={StaticResource BoolToStatusConverter}}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
